Soundboard – ready to upload to GitHub Pages.

Missing audio:
- luv.mp3 is NOT included yet.
  When you have it, drop it into /sounds and remove the 'disabled' attribute
  from the luv button in index.html.

Local test:
  python -m http.server 8000